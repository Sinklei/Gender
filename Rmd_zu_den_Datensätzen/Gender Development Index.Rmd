---
title: "Gender Gevelopment Index Datensatz"
author: "Linda Sinani"
date: "16 7 2021"
output: html_document
---


## Gender Development Index - was zeigt der Datensatz?

misst Ungleichheiten auf dem HDI (Human-DDevelopment Index) nach Geschlecht. 
Datensatz enthält HDI-Werte, die getrennt für Frauen und Männer geschätzt werden; das Verhältnis davon ist der GDI-Wert. 
Je näher der Wert bei 1 liegt, desto geringer ist der Unterschied zwischen Frauen und Männern. 
Die Werte für die drei HDI-Komponenten: 
 Lebenserwartung
 Bildung (mit zwei Indikatoren)
 Einkommen 
werden ebenfalls nach Geschlecht dargestellt. 
Datensatz enthält fünf Ländergruppierungen nach absoluter Abweichung von der Geschlechterparität bei den HDI-Werten.

WERTE VON 2019 außer letzten beiden Variablen (2017)

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)

library(tidyverse)

GDI <- readr:: read_csv(file= "data/raw/GDI.csv")

GDI[ ,3:5] <- round(GDI[, 3:5], digits = 3)
GDI[ ,6:11]<- round(GDI[, 6:11], digits = 1)

```
